import brandLogo from 'assets/images/infogito-logo.png';
import mascotVideo from 'assets/videos/video-mascot-loop.webm';

export const BRAND_NAME = 'infogito';
export const BRAND_EMAIL = 'dispatch@infogito.local';
export const BRAND_LOGO_SRC = brandLogo;
export const MASCOT_NAME = 'Disco';
export const MASCOT_VIDEO_SRC = mascotVideo;
export const MASCOT_QUIPS = [
  "If the TMS and the spreadsheet are both the source of truth, that's not ops. That's improv.",
  "Packet loss and late loads have the same root cause: someone said, 'We'll fix it in dispatch.'",
  "That route has more hops than your network stack. Pick one lane and commit.",
  "If the logs sound like a Jake brake, the integration is not healthy.",
  "Manual updates are cute right up until a ghost load books itself again.",
];
